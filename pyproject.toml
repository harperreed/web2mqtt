[project]
name = "web2mqtt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.3",
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.0.1",
]
